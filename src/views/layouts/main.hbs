<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/styles.css" />
    <title>Home Page</title>
</head>

<body>

    <div>
        {{#each errorMessages}}
        <div class="errorContainer">
            <p>Error</p>
            {{/each}}
        </div>

    </div>

    <div id="wrapper">
        <header>
            <!-- Navigation -->
            <nav>
                <div>
                    <a href="/">Home</a>
                    <a href="/posts/all">Dashboard</a>
                    {{#if isAuthenticated}}
                    <a href="/posts/search">Search</a>
                    {{/if}}
                </div>

                <!-- Logged-in users -->
                {{#if isAuthenticated}}
                <div class="user">
                    <a href="/posts/create">Add Animal</a>
                    <a href="/users/logout">Logout</a>
                </div>
                {{else}}
                <!-- Guest users -->
                <div class="guest">
                    <a href="/users/login">Login</a>
                    <a href="/users/register">Register</a>
                    {{/if}}
                </div>
            </nav>
        </header>


        {{{body}}}
        <footer>
            <p>@Friendly World</p>
        </footer>
    </div>
</body>

</html>
